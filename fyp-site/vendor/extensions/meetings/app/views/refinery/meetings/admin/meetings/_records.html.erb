<%= render 'refinery/admin/search_header', :url => refinery.meetings_admin_meetings_path %>
<div class='pagination_container'>
  <% if @meetings.any? %>
    <%= render 'meetings' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
